{
  "name": "industrilasappen",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/calculator.test.mjs",
    "check": "node --check app.js && node --check calculator.js && node --check sw.js"
  }
}
