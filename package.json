{
  "name": "industrilasappen",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check app.js && node --check calculator.js && node --check planning.js && node --check sw.js"
  }
}
 
